package version

var(
	//BuildTime is time label of the moment when the binary was build
	BuildTime = "unset"
	// Commit is the last commit has at the moment when the binary was built
	Commit = "unset"
	// Release is 
	Release = "unset"
)